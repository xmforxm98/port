import React from "react";

export const ProcessFlow: React.FC = () => (
  <div
    style={{
      display: 'none' // Hide this component as we will use the image directly
    }}
  >
    {/* Previous code for drawing the diagram is now hidden/removed */}
  </div>
);

export default ProcessFlow; 